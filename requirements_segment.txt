# ---------------------------------------------------------------------------
# YOLO 分割（segment/yolo_seg_backend.py）可选依赖
# ---------------------------------------------------------------------------
# 安装顺序：先按 requirements.txt 装好 PyTorch（与 CUDA 对应的官方 wheel）、
# numpy、opencv 等主依赖，再装本文件，避免 pip 擅自升级 torch。
#
# 参考环境（仓库内曾验证过的组合）：
#   torch==2.1.0+cu121
#   numpy==1.26.3
#   opencv-python==4.9.0.80
#
# ultralytics 说明：
#   - 需 torch>=1.8；与 torch 2.1 兼容。
#   - 上限 <8.5 用于规避未来大版本放宽 torch 上界后，pip 可能升级 PyTorch
#     导致与现有 cu121 构建不一致；若你确认要更新 torch，可放宽上限。
#   - 若 pip 仍尝试升级 torch，请使用：
#       pip install -r requirements_segment.txt --upgrade-strategy only-if-needed
#     或显式约束： pip install "ultralytics>=8.2.0,<8.5.0" "torch==2.1.0"
#
pip install "ultralytics>=8.2.0,<8.5.0"